Template.show_task_row.gestures({
   /*'swiperight .task-row': function() {
        Meteor.call('completeTask', this.data._id);
    },
    'swipeleft .task-row': function() {
        Meteor.call('setDueDate', this.data._id, moment().add(1, 'd').toDate());
    }*/ 
});
